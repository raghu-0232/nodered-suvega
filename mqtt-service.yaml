apiVersion: v1
kind: Service
metadata:
  name: mqtt-service
  namespace: nodered
spec:
  type: NodePort
  selector:
    app: mqtt
  ports:
    - name: mqtt
      port: 5000
      targetPort: 1880
      nodePort: 31206
